// if using CommonJS (postcss.config.cjs)
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    // if you want autoprefixer (optional, but older docs included it)
    autoprefixer: {},
  },
}
